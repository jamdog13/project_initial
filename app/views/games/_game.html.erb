<% targ = games_path + "/" + game.id.to_s %>
			<%=link_to game.name, targ%>  Times played: <%=Score.where(game_id:game.id).count%> <!--%=game.times_played%--> High Score: 
			<% @highscore=Score.where(game_id:game.id).order(value: :desc).first %>
			<%= render @highscore %>
			